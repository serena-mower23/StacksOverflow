export default class Model {
    constructor(type, id) {
        this.initialize(type, id)
        this.type = type
        this.id = id
    }
    
    initialize(type, id) {
        if (type === "Designer") {

        }
        else if (type === "Supporter") {

        }
        else if (type === "Admin") {
            
        }
    }
}